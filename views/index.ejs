<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>
  <ul>
    <% messages.forEach((message) => { %>
      <li>
        <strong><%= message.user %>:</strong>
        <p><%= message.text %></p>
        <small>Added on: <%= message.added.toDateString() %></small>
      </li>
    <% }); %>
  </ul>
  <form method="post" action="/new">
    <input type="text" name="author" id="" placeholder="Auther Name">
        <br>
    <textarea name="messege" id="" placeholder="right ur messge ...."></textarea>
        <br>
    <button type="submit">Submitt</button>
  </form>
</body>
</html>
